# JavaScript DOM Manipulation

This project introduces basic JavaScript DOM manipulation.

The DOM, or Document Object Model, is the browser’s representation of an HTML page. JavaScript can use the DOM to select elements, change their content, update their styles, and react to user actions.