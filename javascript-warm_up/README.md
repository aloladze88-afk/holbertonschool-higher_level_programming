# JavaScript - Warm up

This project introduces the basics of JavaScript scripting with Node.js. It covers how to run JavaScript files from the command line, print output with `console.log`, create variables and constants, and follow simple style rules.

The first tasks focus on basic syntax before moving on to conditions, loops, functions, command-line arguments, and imports. The goal is to become comfortable writing small JavaScript scripts outside the browser.
