# Python - Object-relational mapping

This project introduces how Python can interact with a MySQL database. It covers connecting to a database, executing SQL queries from a Python script, retrieving results, and displaying them correctly.

The goal is to understand the basic link between Python code and relational databases before moving towards object-relational mapping tools such as SQLAlchemy.