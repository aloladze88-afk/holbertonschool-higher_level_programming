# Python - Serialization

This project introduces serialization in Python. Serialization means converting Python data, such as dictionaries, lists, or objects, into a format that can be saved or transferred. In this project, we use JSON to store data in files and load it back into Python when needed.
